name: Build and Release

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  DOTNET_VERSION: '8.0.x'
  NUGET_SOURCE: 'https://api.nuget.org/v3/index.json'

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0 # Important for versioning
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
    
    # Build + test the complete project graph from a clean checkout using the
    # SAME script CI and local developers run. If any source or test project in
    # the graph fails, this step (and therefore the whole build job) fails, and
    # the publish-* jobs below cannot run because they declare `needs: build`.
    - name: Build complete graph and run test projects (Release)
      run: scripts/ci-graph-test.sh --configuration Release --require-clean

    - name: Pack
      run: |
        # Determine version
        if [[ $GITHUB_REF == refs/tags/v* ]]; then
          VERSION=${GITHUB_REF#refs/tags/v}
        else
          VERSION=$(date +%Y.%m.%d)-rc.$GITHUB_RUN_NUMBER
        fi

        echo "VERSION=$VERSION" >> $GITHUB_ENV

        # Andy.Tui is the only public package. Its project bundles every source
        # assembly, including CLI widgets, into one installable .nupkg.
        dotnet pack src/Andy.Tui/Andy.Tui.csproj \
          --configuration Release \
          --output ./artifacts \
          -p:PackageVersion=$VERSION

        PACKAGES=(./artifacts/*.nupkg)
        # NuGet normalizes numeric version components (for example 07 -> 7),
        # so validate the package ID rather than comparing the raw filename.
        if [[ ${#PACKAGES[@]} -ne 1 || ! "$(basename "${PACKAGES[0]}")" =~ ^Andy\.Tui\.[0-9].*\.nupkg$ ]]; then
          echo "Expected exactly one versioned Andy.Tui package; found:"
          printf '%s\n' "${PACKAGES[@]}"
          exit 1
        fi

        echo "Produced package: ${PACKAGES[0]}"
    
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: nuget-packages
        path: ./artifacts/*.nupkg
        retention-days: 7

  publish-prerelease:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Download artifacts
      uses: actions/download-artifact@v4
      with:
        name: nuget-packages
        path: ./artifacts
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
    
    - name: Publish to NuGet
      run: |
        PACKAGES=(./artifacts/*.nupkg)
        if [[ ${#PACKAGES[@]} -ne 1 || ! "$(basename "${PACKAGES[0]}")" =~ ^Andy\.Tui\.[0-9] ]]; then
          echo "Refusing to publish: expected exactly one Andy.Tui package."
          printf '%s\n' "${PACKAGES[@]}"
          exit 1
        fi
        dotnet nuget push "${PACKAGES[0]}" \
          --source ${{ env.NUGET_SOURCE }} \
          --api-key ${{ secrets.NUGET_API_KEY }} \
          --skip-duplicate

  publish-release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    
    steps:
    - name: Download artifacts
      uses: actions/download-artifact@v4
      with:
        name: nuget-packages
        path: ./artifacts
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
    
    - name: Publish to NuGet
      run: |
        PACKAGES=(./artifacts/*.nupkg)
        if [[ ${#PACKAGES[@]} -ne 1 || ! "$(basename "${PACKAGES[0]}")" =~ ^Andy\.Tui\.[0-9] ]]; then
          echo "Refusing to publish: expected exactly one Andy.Tui package."
          printf '%s\n' "${PACKAGES[@]}"
          exit 1
        fi
        dotnet nuget push "${PACKAGES[0]}" \
          --source ${{ env.NUGET_SOURCE }} \
          --api-key ${{ secrets.NUGET_API_KEY }}
    
    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: ./artifacts/*.nupkg
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
